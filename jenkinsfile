pipeline {
    agent {
        label 'worker'
    }
    stages {
        stage('checkout') {
           steps { 
            sh '''
            git clone 'https://github.com/shivnandan05/studentapp-ui.git'

            '''
            }
        }
    }
}

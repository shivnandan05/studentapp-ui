pipeline {
    agent {
        label 'worker'
    }
    stages {
        stage('checkout') {
           steps { 
           checkout scm
            }
        }
        stage ('Build') {
           steps { 
            sh 'ls -la'
            } 
        }
    }
}
